# Lint 豁免登记表

> `make lint`（VCS +lint；SpyGlass 部署后换后端、入口与本表不变）告警的处置：修复，或在此逐条登记豁免。
> 登记人 = DE；**复核人 = rev**（未复核的豁免不算数）。格式仿覆盖率过滤登记：对象/位置/原因/结论。
> 已批准豁免超限时 `make docs-archive` 滚动归档到 lint-waivers-archive.md（保留最新 2 条；待复核行不归档），归档行豁免效力不变，核对时 grep 两个文件。

| # | 告警类别 | 对象（文件:行） | 原因 | 结论（豁免/待修） | 复核（rev/日期） |
| --- | --- | --- | --- | --- | --- |
| 7 | Lint-[NS] Null statement | tb/uvm/env/m3_stub_driver.sv:26,35,41,43,50,52,57,59,71,74,77,87（`@(vif.drv_cb);` 时钟块同步等待，`set_result`/`set_busy`/`pulse_done`/`clear_done`/`read_sram`/`watch_start_pulse` 等 task 内） | 与 #5 同一类别、同一根因：DV 撰写的 M3 桩驱动组件用标准 UVM clocking-block 同步等待驱动 stub 激励，`+lint=all` 对"仅含时序控制、无其他动作"的 `@(...)` 语句提示 Null statement，去掉即破坏与 apb_slave_if M3 结果输入 / packet_sram 读口 / start_o 观测窗口的同拍驱动时序。0.1.6 首批（8 处）为 rev 已批准范围；0.1.7 覆盖率收窄新增 `read_sram`（71,74,77）/`watch_start_pulse`（87）共 4 处，同一根因追加登记，rev 逐行核对四处均为独立同步等待、去掉即破坏同拍驱动时序，无隐藏真实缺陷 | 豁免（全 12 处） | rev 2026-07-09 批准（首批8处）；追加 4 处 rev 2026-07-10 批准（见 doc/evidence/v0.1.9/rev-review-toggle-lint7.md） |
| 8 | Lint-[SVA-DIU] Disable iff used | rtl/packet_proc_core.sv:278,283,288,293,298,304,309,317,322（a_state_legal/a_busy_done_mutex/a_rden_only_process/a_word_cnt_bound/a_word_cnt_incr/a_process_ignores_start/a_process_outputs_clear/a_algo_mode0_no_chkerr/a_format_ok_consistency 九条 M3 内部不变量断言） | 与 #1/#2（已归档，rtl/packet_sram.sv、rtl/apb_slave_if.sv）同一类别、同一写法：`disable iff` 为复位期间屏蔽断言评估的标准/推荐写法，已用单一信号 `rst`（`assign rst = !rst_n;`，模块内定义于 `` `ifndef SYNTHESIS`` 块）规避更实质的 `Lint-[SVA-CE] Complex expression found` 告警，仅余提示性的 `Lint-[SVA-DIU]`，九处均为等价必要用法，无法进一步消除又不破坏断言语义。本次 `make -C sim compile`（临时启用 `sim/flist/rtl.f` 中 `../rtl/packet_proc_core.sv` 一行以便自检，模块尚未接入 tb_top，未实例化）0 error/0 warning；`make -C sim lint` 判定范围内（../rtl/ ../tb/）除本条 9 处外无新增发现 | 豁免（全 9 处，登记人=DE） | 待 rev 复核 |

（注：本次自检除上表 4 条外未见其余 `Lint-` 告警指向 rtl/packet_sram.sv、rtl/apb_slave_if.sv、tb/sva/apb_protocol_sva.sv、tb/sva/apb_slave_if_sva.sv；tb/ 下既有 `Lint-[NS]` `Lint-[ULCO]` `Lint-[WMIA-L]` 等告警属既有 TB/UVM 骨架代码（0.1.0），非本次交付范围，未登记于此表（见 doc/bugs.md BUG-006）。`make -C sim lint` 因 sim/flist 顺序问题实测报错（rtl.f 早于 tb.f 编译，package 未就绪），详见 BUG-005；本表结论基于诊断性验证——手动执行 `vcs +lint=all,noVCDE -f flist/tb.f -f flist/rtl.f`（仅调换清单顺序，未改 Makefile/flist 任何文件）——两次分别针对 packet_sram.sv、apb_slave_if.sv 均未见 `Error-` 级别发现，退出码 0。#3/#4 为本轮 DV 新增交付，`make -C sim lint`（2026-07-09 本地 VM 实测）在收窄后的判定范围内除 #1~#4 的 `Lint-[SVA-DIU]` 外无其余 `Error-`/新增 `Lint-` 发现；#3/#4 登记人=DV，待 rev 复核批准。#5/#6 为 BUG-006 处置：DV 于 2026-07-09 复核确认 6 条 `Lint-[NS]` + 6 条 `Lint-[WMIA-L]` 均属 0.1.0 遗留、语义必要/库内部实现，登记豁免（登记人=DV，复核人=rev，留空待复核，不自行批准）；同批次 `Lint-[ULCO]`（tb/uvm/env/ppa_ref_model.sv:47/68/70，共 6 处子表达式）判定为可修复告警，已直接在源码加显式 `int'(...)` 类型转换消除，不登记豁免——详见 doc/bugs.md BUG-006 行根因/裁决列。DV 处置后 `make -C sim lint` 复跑仅余本表 #1~#6 共 27 条 `Lint-[SVA-DIU]`/`Lint-[NS]`/`Lint-[WMIA-L]`，`Lint-[ULCO]` 已清零，`Lint-` 判定范围内（../rtl/ ../tb/）无其余发现，另观察到 `tb/uvm/env/m3_stub_driver.sv`（未纳入版本控制的新文件，8 处 `Lint-[NS]`）不在 BUG-006 登记范围内，不属于本次处置对象，留待该文件提交时另行处置）
