# Lint 豁免登记表

> `make lint`（VCS +lint；SpyGlass 部署后换后端、入口与本表不变）告警的处置：修复，或在此逐条登记豁免。
> 登记人 = DE；**复核人 = rev**（未复核的豁免不算数）。格式仿覆盖率过滤登记：对象/位置/原因/结论。
> 已批准豁免超限时 `make docs-archive` 滚动归档到 lint-waivers-archive.md（保留最新 2 条；待复核行不归档），归档行豁免效力不变，核对时 grep 两个文件。

| # | 告警类别 | 对象（文件:行） | 原因 | 结论（豁免/待修） | 复核（rev/日期） |
| --- | --- | --- | --- | --- | --- |
| 9 | Lint-[SVA-DIU] Disable iff used | rtl/ppa_top.sv:166,170,174,178,182,187（a_no_x_pkt_mem_we/a_no_x_mem_rd_en/a_no_x_busy/a_no_x_done/a_no_x_start/a_rd_data_direct 六条 Top 连通性/无 X 内部断言） | 与 #8（rtl/packet_proc_core.sv）及已归档 #1/#2（rtl/packet_sram.sv、rtl/apb_slave_if.sv）同一类别、同一根因、同一写法：`disable iff` 为复位期间屏蔽断言评估的标准/推荐写法，已用单一信号 `rst`（`assign rst = !PRESETn;`，定义于 `` `ifndef SYNTHESIS`` 块）规避更实质的 `Lint-[SVA-CE]` 告警，仅余提示性的 `Lint-[SVA-DIU]`；六处均为等价必要用法，无法进一步消除又不破坏断言语义。ppa_top 另四条时钟/复位连通性断言（a_sram_clk_conn/a_core_clk_conn/a_sram_rst_conn/a_core_rst_conn）不用 `disable iff`、不触发本告警。本次 2026-07-14 本地 VM 实测 `make -C sim compile` 0 error（All of 20 modules done，KDB 0 error/0 warning），`make -C sim lint` 判定范围内（../rtl/ ../tb/）ppa_top.sv 仅此 6 处 `Lint-[SVA-DIU]`、无其余类别告警（含未连接输出 enable_o/done_irq_en_o/err_irq_en_o 未触发任何 Lint 告警） | 豁免（全 6 处，登记人=DE） | rev 2026-07-14 批准 |
| 10 | Lint-[NS] Null statement | tb/uvm/test/m3_seq_lib.sv:329,344（`@(top_vif.mon_cb);` 时钟块同步等待，`m3_irq_seq.body` 内 irq_o=1 / irq_o=0 两处观测点前的对齐等待） | 与 #7（tb/uvm/env/m3_stub_driver.sv 的 `@(vif.drv_cb);`）及 #5（已归档）完全同一类别、同一根因：M3-05 中断路径闭环序列需在时钟沿采样顶层 irq_o（§8.2 组合输出），用标准 clocking-block 同步等待 `@(top_vif.mon_cb);` 对齐采样点，`+lint=all` 对"仅含时序控制、无其他动作"的 `@(...)` 语句提示 Null statement；去掉即无法对齐 irq_o 采样时刻（提前/滞后一拍读到过渡值），为等价必要用法，无隐藏真实缺陷 | 豁免（全 2 处，登记人=DV，待 rev 复核） | 待 rev 复核 |

（注：本次自检除上表 4 条外未见其余 `Lint-` 告警指向 rtl/packet_sram.sv、rtl/apb_slave_if.sv、tb/sva/apb_protocol_sva.sv、tb/sva/apb_slave_if_sva.sv；tb/ 下既有 `Lint-[NS]` `Lint-[ULCO]` `Lint-[WMIA-L]` 等告警属既有 TB/UVM 骨架代码（0.1.0），非本次交付范围，未登记于此表（见 doc/bugs.md BUG-006）。`make -C sim lint` 因 sim/flist 顺序问题实测报错（rtl.f 早于 tb.f 编译，package 未就绪），详见 BUG-005；本表结论基于诊断性验证——手动执行 `vcs +lint=all,noVCDE -f flist/tb.f -f flist/rtl.f`（仅调换清单顺序，未改 Makefile/flist 任何文件）——两次分别针对 packet_sram.sv、apb_slave_if.sv 均未见 `Error-` 级别发现，退出码 0。#3/#4 为本轮 DV 新增交付，`make -C sim lint`（2026-07-09 本地 VM 实测）在收窄后的判定范围内除 #1~#4 的 `Lint-[SVA-DIU]` 外无其余 `Error-`/新增 `Lint-` 发现；#3/#4 登记人=DV，待 rev 复核批准。#5/#6 为 BUG-006 处置：DV 于 2026-07-09 复核确认 6 条 `Lint-[NS]` + 6 条 `Lint-[WMIA-L]` 均属 0.1.0 遗留、语义必要/库内部实现，登记豁免（登记人=DV，复核人=rev，留空待复核，不自行批准）；同批次 `Lint-[ULCO]`（tb/uvm/env/ppa_ref_model.sv:47/68/70，共 6 处子表达式）判定为可修复告警，已直接在源码加显式 `int'(...)` 类型转换消除，不登记豁免——详见 doc/bugs.md BUG-006 行根因/裁决列。DV 处置后 `make -C sim lint` 复跑仅余本表 #1~#6 共 27 条 `Lint-[SVA-DIU]`/`Lint-[NS]`/`Lint-[WMIA-L]`，`Lint-[ULCO]` 已清零，`Lint-` 判定范围内（../rtl/ ../tb/）无其余发现，另观察到 `tb/uvm/env/m3_stub_driver.sv`（未纳入版本控制的新文件，8 处 `Lint-[NS]`）不在 BUG-006 登记范围内，不属于本次处置对象，留待该文件提交时另行处置）
