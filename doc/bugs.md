# 缺陷登记表

> 流程见 CLAUDE.md §4.3：发现即登记（禁止只在对话里传递）→ orch 派单 → 修复回填 → **复验关单（关单人 ≠ 修复人）**。
> 状态：`OPEN / FIXING / FIX_READY / VERIFYING / CLOSED / TB_BUG / SPEC_CHANGED / WONTFIX`。
> CLOSED 必须填复验证据（doc/evidence/ 路径，docs-check 校验）。最小复现写完整命令（含 TEST+SEED）。
> 终态行超限时 `make docs-archive` 滚动归档到 bugs-archive.md（保留最新 2 条终态行；活跃缺陷不归档）；追溯历史缺陷 grep 归档件。

| ID | 日期 | 版本 | 疑似归属 | 现象摘要 | 最小复现 | spec 依据 | 根因/裁决 | 修复 commit | 状态 | 复验证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-016 | 2026-07-26 | 0.5.1 | TB（技术债：死代码 + 双份参考模型静默漂移风险） | `tb/uvm/env/ppa_ref_model.sv` 的 `golden_calc()` **零调用者**（`git grep "golden_calc" tb/` 的唯一非定义命中是 ppa_scoreboard.sv 里一条已撤销的 TODO）。而期望值计算实际由 `tb/uvm/core_agent/ppa_core_seq_item.sv::predict()` 内联另实现了一份。**两份都自称"从 spec 推导"，构成静默漂移风险**：改一份忘另一份，而没有调用者的那一份永远不会被仿真证伪，却仍以"参考模型"的名义存在于环境里，误导读者（也误导了 BUG-013 的任务卡——orch 曾把 ppa_ref_model 列为实际检查落点，被 DV 实查推翻） | `git grep -n "golden_calc" tb/`（静态）。发现人=DV（BUG-013 处置实例，超出任务卡范围故只列清单未擅改），2026-07-26 | CLAUDE.md §2（UVM 一类一文件、环境组件应有实际职责）、§4.2（检查器只准从 spec 推导——双份实现使"从 spec 推导"不可验证） | 待处置：二选一由 orch 定 scope——(a) 删除 `ppa_ref_model.sv` 并从 `ppa_env_pkg.sv` 摘除，期望值单点收敛到 `predict()`；(b) 让 `predict()` 改调 `golden_calc()`，参考模型单点收敛到 ppa_ref_model（与 ppa_scoreboard 注释里写的"集中式比对后续演进项"同向，代价更大）。任一路径改完须 `make regress` 32/32 零回归。**rev 复验关单（关单人=rev≠修复人=DV，2026-07-27，被审 HEAD 7616e35）**：取路径 (a)——`tb/uvm/env/ppa_ref_model.sv` 已删、`ppa_env_pkg.sv` 摘除 include、scoreboard 与 report.py H1 三处注释改指 predict() 单点。独立复核：`git grep "ppa_ref_model\|golden_calc" tb/ sim/ scripts/` 唯一命中 report.py:263 的历史性陈述（"已按 BUG-016 删除"，准确）；`predict()` 定义 ppa_core_seq_item.sv:65、唯一调用者 ppa_core_driver.sv:39，全 tb/ 无第二份独立参考模型文件（m4_seq_lib:21 是自检序列内联 spec 期望值，非模型文件），期望值单点收敛成立；本实例独立实跑 `python3 scripts/regress.py` 得 **32/32 PASS**（含全编 0 error）。审查明细见 doc/evidence/v0.5.5/review-bug-015-016-017.md §B。**关单** | 129ce6f | CLOSED | doc/evidence/v0.5.5/review-bug-015-016-017.md |
| BUG-017 | 2026-07-26 | 0.5.3 | infra（svacheck 断言拦截存在三条可绕过向量——BUG-014 修复的收尾加固） | rev 复验 BUG-014 时构造出**三条能骗过 `scripts/svacheck.py` 的漏报向量**：① **不校验 attempts/总数基线**（高危）——`$assertoff` 或把 `tb/sva/*.sv` 摘出 flist 后，log 呈 `Summary: 2 assertions, 0 with attempts, 0 with failures`，svacheck 判 CLEAN 退出码 0，真违例、回归全绿；信息就在同一行里（断言总数/尝试数骤降）却被忽略；② **层 1 对类作用域断言的 `::` 层次名结构性失明**——失败行层次名形如 `p::\chk::check .unnamed$$_0` 时，正则 hier 段 `[^\s:]+` 命中 0，仅靠层 2（`-assert verbose` 汇总）兜住，即 svacheck.py 文件头「层 1 不依赖任何编译选项、fail-closed」的自述对该类**不成立**；③ **`SUMMARY_RE` 取最后一条**——拼接 log「先失败后干净」判 CLEAN。另两条顺带：`evidence.py` 的 `KEY_LINE_RE` 对 core-agent 类测试（M2-0x）无任何关键行命中（摘录信息量弱）；BUG-013 源码注释守卫召回窄（rev 构造 7 条真过期承诺全部逃逸：模块名不用 M<N>、句号切断窗口、跨行折行、英文措辞等），须把召回边界写进 docstring | 复现见 `doc/evidence/v0.5.3/review-bug-013-014.md` §A2/A5（含三条向量的构造语料与真实输出）。发现人=rev（BUG-013/014 关单实例），2026-07-26 | CLAUDE.md §4.2（防验证造假——拦截器自身可被绕过即证据链仍有洞）；BUG-014 条目 | 待处置：派 DV 加固 svacheck——① 增加断言总数/attempts 基线校验（与已知基线 91/88 比对，骤降即 FAIL，基线变化须显式登记）；② 修 `::` 层次名正则并订正 fail-closed 的 docstring 措辞（明确层 1 覆盖不到的类别）；③ `SUMMARY_RE` 逐条检查所有 Summary 行而非取末条；④ 顺带：KEY_LINE_RE 补 core-agent 关键行形态、守卫召回边界写进 docstring。全部改完负向复验三条向量均转 FAIL。**rev 复验关单（关单人=rev≠修复人=DV，2026-07-27，被审 HEAD 7616e35）**：① 三条原向量（含 4 子情形：$assertoff attempts→0 / flist 摘出 total→掉 / :: 类作用域层次名 / 多 Summary 先失败后干净）逐条经 svacheck CLI 重放**全部转 FAIL 退出码 1**；② 基线机制审查+实测——floor(≥) 语义成立（95/90、91/88 均 CLEAN）、svacheck 全文无写盘故"脚本永不自适应"可机械验证、`--no-baseline` 只在 CLI 而 regress/evidence 恒加载基线（不可滥用为常规路径）、**基线文件缺失 fail-closed 实测**（字节备份+改名后 CLI 与 scan_text 双路 SystemExit 退出码 1，还原后 sha256 一致）；③ KEY_LINE_RE 补 running test、report.py R6 召回边界已核；④ 32/32 回归 + 33 out log + 4 covreset log 零误伤。**换角度另构造 2 条新绕过**（均当前不可利用、非三向量残留，另交 orch 登记）：**A**（中）sva_baseline.json 无 sha-pin/无 changelog 机械校验，下调 floor 即静默停用层 3（实测 floor=0/0 时 91/0/0 判 CLEAN），建议纳入 docs-check 守卫；**B**（低）层 1 `FAIL_LINE_RE` 尾锚 `\s*\r?$` 对 VCS 真实 `Offending '<sig>'` 尾巴（本项目 BUG-014 登记的 a_done_hold 失败行即此格式）失明、层 1b 对无动作块断言亦失明——当前因 49 条并发断言全带 else $error（层 1b 兜）+ verbose 恒开（层 2 兜）+ 缺汇总行 fail-closed 而不可利用，但证伪 svacheck 文件头"并发断言全被层 1 锚住、不依赖编译选项"之自述，建议放宽尾锚并再收窄 docstring。审查明细见 doc/evidence/v0.5.5/review-bug-015-016-017.md §C。**关单** | 129ce6f | CLOSED | doc/evidence/v0.5.5/review-bug-015-016-017.md |
| BUG-018 | 2026-07-27 | 0.5.5 | infra（svacheck 二轮加固——BUG-017 关单 rev 新构造的两条绕过向量） | BUG-015/016/017 关单 rev（≠BUG-017 修复人）按卡内要求换角度构造新绕过，得两条（当前均不可利用，故不阻断 017 关单，但须堵）：**A（中危）基线文件无守卫**——`sim/regress/sva_baseline.json` 无 sha-pin、无 docs-check 内容校验、无 changelog 机械核对（全仓只有 spec.md 被 pin），实测把 floor 静默下调为 0/0 后，`$assertoff` 向量（91/0/0）重新判 CLEAN——层 3 的信任锚可被一次静默编辑废掉，仅靠 /closeout 人工 diff 兜底，与基线文件自称"硬规则：只准人工编辑+changelog 留痕"名不副实；**B（低危）层 1 尾锚失明**——`FAIL_LINE_RE` 尾锚 `\s*\r?$` 对 VCS 真实产出的 `... failed at <t>  Offending '<sig>'` 同行尾巴格式失明（此格式正是 BUG-014 登记的 a_done_hold 真实失败行形态），层 1b 对无动作块断言亦失明；当前仓库不可利用（49 条并发断言全带 else $error 由层 1b 兜 + verbose 恒开由层 2 兜 + 缺汇总行 fail-closed），但**证伪了 svacheck.py 文件头"并发断言全被层 1 锚住、不依赖任何编译选项"的自述**——正是 BUG-017 R2 要修的同类过宽表述在修复中再次出现 | 构造语料与真实输出见 `doc/evidence/v0.5.5/review-bug-015-016-017.md`（A：字节备份基线→改 0/0→重放 $assertoff 向量判 CLEAN→还原 sha256 一致；B：真实 a_done_hold 失败行喂层 1 不命中）。发现人=rev（BUG-015/016/017 关单实例），2026-07-27 | CLAUDE.md §4.2（防验证造假——守卫的信任锚本身须受守卫）；BUG-017 条目 | 待处置：派 DV ① 把 `sva_baseline.json` 纳入机械守卫——建议在 `report.py --check` 增加基线文件内容校验（sha 登记或"floor 值变更须同 commit 追 changelog 行"的机械核对；不动 docs.py）；② 放宽 `FAIL_LINE_RE` 尾锚吃掉同行 Offending 尾巴，并第三次收窄 svacheck.py 文件头自述（逐层如实写明各自盲区，不再作"全覆盖"声明）；③ 负向复验：A/B 两条语料均转 FAIL/命中 | 59c07f1 | FIX_READY | - |
