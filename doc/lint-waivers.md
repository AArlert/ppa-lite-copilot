# Lint 豁免登记表

> `make lint`（VCS +lint；SpyGlass 部署后换后端、入口与本表不变）告警的处置：修复，或在此逐条登记豁免。
> 登记人 = DE；**复核人 = rev**（未复核的豁免不算数）。格式仿覆盖率过滤登记：对象/位置/原因/结论。
> 已批准豁免超限时 `make docs-archive` 滚动归档到 lint-waivers-archive.md（保留最新 2 条；待复核行不归档），归档行豁免效力不变，核对时 grep 两个文件。

| # | 告警类别 | 对象（文件:行） | 原因 | 结论（豁免/待修） | 复核（rev/日期） |
| --- | --- | --- | --- | --- | --- |
| 10 | Lint-[NS] Null statement | tb/uvm/test/m3_seq_lib.sv:329,344（`@(top_vif.mon_cb);` 时钟块同步等待，`m3_irq_seq.body` 内 irq_o=1 / irq_o=0 两处观测点前的对齐等待） | 与 #7（tb/uvm/env/m3_stub_driver.sv 的 `@(vif.drv_cb);`）及 #5（已归档）完全同一类别、同一根因：M3-05 中断路径闭环序列需在时钟沿采样顶层 irq_o（§8.2 组合输出），用标准 clocking-block 同步等待 `@(top_vif.mon_cb);` 对齐采样点，`+lint=all` 对"仅含时序控制、无其他动作"的 `@(...)` 语句提示 Null statement；去掉即无法对齐 irq_o 采样时刻（提前/滞后一拍读到过渡值），为等价必要用法，无隐藏真实缺陷 | 豁免（全 2 处，登记人=DV） | 批准（rev/2026-07-16）：L329/L344 均 `@(top_vif.mon_cb);` clocking-block 同步等待，对齐 irq_o 组合输出采样拍（§8.2），语义必要、无孤儿语句体、无隐藏缺陷（M3-05 PASS 背书）。见 doc/evidence/v0.4.1/review-m4-milestone.md C 项 |
| 11 | Lint-[NS] Null statement | tb/uvm/test/m4_seq_lib.sv:309,311（`m4_reset_integ_seq.inject_rst` 内 `repeat(N) @(top_vif.mon_cb);` 复位注入拍数对齐）+ tb/uvm/core_agent/ppa_core_driver.sv:29,30,47,52,63,76,81,88,90,110（`@(vif.drv_cb);` 时钟块同步等待，含 M4-02d `inject_reset` 复位注入拍对齐）。M4-02 覆盖率闭环新增/扩充 | 与 #7/#10 完全同一类别、同一根因：M4-02d/e 运行中复位注入需在时钟沿精确对齐"拉低 force_rst_n → 保持 N 拍 → 释放"的拍数与复位后采样点，用标准 clocking-block `@(...)` 同步等待；`+lint=all` 对仅含时序控制的 `@(...)` 提示 Null statement，去掉即无法对齐复位注入/采样时刻，等价必要用法、无隐藏真实缺陷（复位注入功能正确性由测试内 UVM 断言 + STATUS 清零核对被动保证，主回归 ppa_m2_09/ppa_m3_07 均 PASS）。ppa_core_driver.sv 多数 `@(vif.drv_cb)` 为 M2 既有 driver 握手节拍，本次仅新增 inject_reset 的同类等待 | 豁免（登记人=DV） | 批准（rev/2026-07-16）：m4_seq_lib L309/311 `repeat(N) @(...)` 复位注入拍对齐 + ppa_core_driver 诸 `@(vif.drv_cb);` 同步等待，均标准 clocking-block 时序控制、无孤儿语句体、无隐藏缺陷（ppa_m2_09/ppa_m3_07 reset PASS + STATUS 清零背书）。见 review-m4-milestone.md C 项。**注记撤销（DV/2026-07-26，依据 BUG-012 实测复核）**：原注记「对象列所记 ppa_core_driver『29』实为 L30（行号微差、建议顺手订正）」有误——实地读取 tb/uvm/core_agent/ppa_core_driver.sv 源码确认 L29 为 `wait (vif.rst_n === 1'b1);`（纯时序等待语句，本身即独立的 Lint-[NS] 告警点），L30 为 `@(vif.drv_cb);`，二者是两处独立语句、独立告警，`make -C sim lint` 实测 L29/L30 均出现在告警列表中；原登记对象列 10 处（29,30,47,52,63,76,81,88,90,110）逐一核对无误，故撤销该笔误注记，不作任何行号改动 |
| 12 | Lint-[SVA-DIU] Disable iff used | tb/sva/packet_proc_core_sva.sv:24,29,34,40,45,51,56（a_busy_after_start/a_busy_done_excl/a_rden_only_in_process/a_process_len_clamp/a_done_hold/a_restart_clears/a_format_ok_def 七条 packet_proc_core 接口/协议 SVA，bind 到 rtl/packet_proc_core.sv，仅引用模块端口信号） | 与 #3/#4（已批准，tb/sva/apb_protocol_sva.sv、tb/sva/apb_slave_if_sva.sv）同一类别、同一根因、同一写法：核对 tb/sva/packet_proc_core_sva.sv L18-20 确认与 #3/#4 完全一致的写法——用单一信号 `rst`（`assign rst = !rst_n;`）规避更实质的 `Lint-[SVA-CE] Complex expression found` 告警，仅余提示性的 `Lint-[SVA-DIU]`；七条均为 DV 交付的 bind SVA（tb/sva/README.md 约定：接口/协议/时序契约 SVA 归 DV），`disable iff` 用于复位期间屏蔽断言评估，为等价必要用法，无法进一步消除又不破坏断言语义。本条系 BUG-012 补登记：`make -C sim lint`（2026-07-26 本地 VM 实测）在既有 #1~#11 登记范围外新发现本条 7 处，属登记遗漏（同根因告警在 #3/#4 已获批），非新增风险 | 豁免（全 7 处，登记人=DV） | 待 rev 复核 |

（注：本次自检除上表 4 条外未见其余 `Lint-` 告警指向 rtl/packet_sram.sv、rtl/apb_slave_if.sv、tb/sva/apb_protocol_sva.sv、tb/sva/apb_slave_if_sva.sv；tb/ 下既有 `Lint-[NS]` `Lint-[ULCO]` `Lint-[WMIA-L]` 等告警属既有 TB/UVM 骨架代码（0.1.0），非本次交付范围，未登记于此表（见 doc/bugs.md BUG-006）。`make -C sim lint` 因 sim/flist 顺序问题实测报错（rtl.f 早于 tb.f 编译，package 未就绪），详见 BUG-005；本表结论基于诊断性验证——手动执行 `vcs +lint=all,noVCDE -f flist/tb.f -f flist/rtl.f`（仅调换清单顺序，未改 Makefile/flist 任何文件）——两次分别针对 packet_sram.sv、apb_slave_if.sv 均未见 `Error-` 级别发现，退出码 0。#3/#4 为本轮 DV 新增交付，`make -C sim lint`（2026-07-09 本地 VM 实测）在收窄后的判定范围内除 #1~#4 的 `Lint-[SVA-DIU]` 外无其余 `Error-`/新增 `Lint-` 发现；#3/#4 登记人=DV，待 rev 复核批准。#5/#6 为 BUG-006 处置：DV 于 2026-07-09 复核确认 6 条 `Lint-[NS]` + 6 条 `Lint-[WMIA-L]` 均属 0.1.0 遗留、语义必要/库内部实现，登记豁免（登记人=DV，复核人=rev，留空待复核，不自行批准）；同批次 `Lint-[ULCO]`（tb/uvm/env/ppa_ref_model.sv:47/68/70，共 6 处子表达式）判定为可修复告警，已直接在源码加显式 `int'(...)` 类型转换消除，不登记豁免——详见 doc/bugs.md BUG-006 行根因/裁决列。DV 处置后 `make -C sim lint` 复跑仅余本表 #1~#6 共 27 条 `Lint-[SVA-DIU]`/`Lint-[NS]`/`Lint-[WMIA-L]`，`Lint-[ULCO]` 已清零，`Lint-` 判定范围内（../rtl/ ../tb/）无其余发现，另观察到 `tb/uvm/env/m3_stub_driver.sv`（未纳入版本控制的新文件，8 处 `Lint-[NS]`）不在 BUG-006 登记范围内，不属于本次处置对象，留待该文件提交时另行处置）

（BUG-012 补登记说明，DV/2026-07-26）：`make -C sim lint`（本地 VM 实测，log 见 sim/out/lint.log）按 (类别,文件,行号) 去重统计，HEAD 处本仓库范围（`../rtl/`、`../tb/`）共 **84** 处告警，与 doc/bugs.md BUG-012 条目实测数一致；对账 doc/lint-waivers.md + doc/lint-waivers-archive.md 已登记的 74 处（含本次订正 #8 行号后按当前行号匹配），差集为 **10 处**，与 BUG-012 条目所列一致（未发现差异）：① `tb/sva/packet_proc_core_sva.sv:24,29,34,40,45,51,56` 共 7 处 `Lint-[SVA-DIU]`——本表登记为 #12（豁免，与 #3/#4 同根因）；② `tb/uvm/test/ppa_m2_01_test.sv:31`、`tb/uvm/test/ppa_m2_02_test.sv:27,42` 共 3 处 `Lint-[WMIA-L]`——核对代码发现与 #6（UVM 库宏内部实现、不可控）根因不同：本处是测试激励 `foreach` 循环体内 `int` 型循环变量隐式赋给 `bit [7:0] payload[]` 元素产生的宽度不匹配，属于可低风险修复的场景（仓库内 `ppa_m2_08_rand_test.sv`/`ppa_m2_09_reset_test.sv`/`m4_seq_lib.sv` 已有 `8'(...)` 显式截断转换的既定写法），故未登记为豁免，改为直接在源码补 `8'(...)` 显式转换消除（数值范围内无行为变化），`make -C sim regress` 32/32 PASS 确认零回归后确认消除。补登记 + 补修复后 `make -C sim lint` 本仓库范围内复测为 81 处，均已在 #1~#12 登记覆盖）
