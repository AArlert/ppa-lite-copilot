# 缺陷登记表归档件

> **默认不读**，只在追溯历史缺陷时 grep。由 `make docs-archive` 机械搬运 bugs.md 中的终态行
> （CLOSED/TB_BUG/SPEC_CHANGED/WONTFIX），新的在上；活跃缺陷永不入此文件。
> 新缺陷编号前先 `grep -o "BUG-[0-9]*"` 本文件与 bugs.md 取最大 ID，避免复用已归档编号（docs-check 有跨文件查重兜底）。

| ID | 日期 | 版本 | 疑似归属 | 现象摘要 | 最小复现 | spec 依据 | 根因/裁决 | 修复 commit | 状态 | 复验证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-016 | 2026-07-26 | 0.5.1 | TB（技术债：死代码 + 双份参考模型静默漂移风险） | `tb/uvm/env/ppa_ref_model.sv` 的 `golden_calc()` **零调用者**（`git grep "golden_calc" tb/` 的唯一非定义命中是 ppa_scoreboard.sv 里一条已撤销的 TODO）。而期望值计算实际由 `tb/uvm/core_agent/ppa_core_seq_item.sv::predict()` 内联另实现了一份。**两份都自称"从 spec 推导"，构成静默漂移风险**：改一份忘另一份，而没有调用者的那一份永远不会被仿真证伪，却仍以"参考模型"的名义存在于环境里，误导读者（也误导了 BUG-013 的任务卡——orch 曾把 ppa_ref_model 列为实际检查落点，被 DV 实查推翻） | `git grep -n "golden_calc" tb/`（静态）。发现人=DV（BUG-013 处置实例，超出任务卡范围故只列清单未擅改），2026-07-26 | CLAUDE.md §2（UVM 一类一文件、环境组件应有实际职责）、§4.2（检查器只准从 spec 推导——双份实现使"从 spec 推导"不可验证） | 待处置：二选一由 orch 定 scope——(a) 删除 `ppa_ref_model.sv` 并从 `ppa_env_pkg.sv` 摘除，期望值单点收敛到 `predict()`；(b) 让 `predict()` 改调 `golden_calc()`，参考模型单点收敛到 ppa_ref_model（与 ppa_scoreboard 注释里写的"集中式比对后续演进项"同向，代价更大）。任一路径改完须 `make regress` 32/32 零回归。**rev 复验关单（关单人=rev≠修复人=DV，2026-07-27，被审 HEAD 7616e35）**：取路径 (a)——`tb/uvm/env/ppa_ref_model.sv` 已删、`ppa_env_pkg.sv` 摘除 include、scoreboard 与 report.py H1 三处注释改指 predict() 单点。独立复核：`git grep "ppa_ref_model\|golden_calc" tb/ sim/ scripts/` 唯一命中 report.py:263 的历史性陈述（"已按 BUG-016 删除"，准确）；`predict()` 定义 ppa_core_seq_item.sv:65、唯一调用者 ppa_core_driver.sv:39，全 tb/ 无第二份独立参考模型文件（m4_seq_lib:21 是自检序列内联 spec 期望值，非模型文件），期望值单点收敛成立；本实例独立实跑 `python3 scripts/regress.py` 得 **32/32 PASS**（含全编 0 error）。审查明细见 doc/evidence/v0.5.5/review-bug-015-016-017.md §B。**关单** | 129ce6f | CLOSED | doc/evidence/v0.5.5/review-bug-015-016-017.md |
| BUG-013 | 2026-07-26 | 0.5.1 | infra（TB 注释与交付状态失步，非功能缺陷） | 仓库多处注释停留在 0.1.x（M1 期）语境，未随后续交付更新，导致**代码自述与实际交付状态矛盾**：① `tb/tb_top.sv:28,38`、`tb/uvm/env/ppa_env.sv:9`、`tb/m3_stub_if.sv:1` 共 4 处写「M3 尚未交付」，而 `rtl/packet_proc_core.sv`（332 行）已于 0.2.2 交付（commit b4fb27e）、`tb_top.sv` 同时例化了 `ppa_top`(L105) 与 `packet_proc_core`(L125)——m3_stub 保留是为 M1 单元级通路隔离（正确的设计选择），但「尚未交付」的措辞已是事实错误；② `tb/uvm/env/ppa_scoreboard.sv:2,21,22` 自述「骨架阶段只做事务记录；M1 起由 DV 补齐 CSR 镜像比对」并留两条 TODO(M1,DV)/TODO(M3,DV)，而该比对实际是以另一种架构落地的（自检序列 + driver 内建参考模型：m1_seq_lib.sv 36 处带 spec 章节号的期望值比对、ppa_core_driver.sv 33 处、m3/m4_seq_lib 的 chk_eq 共 24 处、ppa_ref_model.sv golden 计算），TODO 从未被撤销或改写，读者会误判为「检查未实现」。后果：任何人打开仓库第一眼即得出「项目未完成」的结论，与 testplan 31/31 ✅、回归 32/32、rev 四次里程碑签核的事实相悖；对外展示材料若在此状态下宣称「M1–M4 全部完成」，会被 `git grep "尚未交付"` 当场证伪 | `git grep -n "尚未交付\|骨架阶段\|TODO(M" tb/`（静态，无需仿真）。发现人=用户（人工审阅本地代码时提出「这个仓库的项目真的已经做完了吗」），2026-07-26；orch 核实四处「M3 尚未交付」措辞与 rtl/packet_proc_core.sv 交付事实矛盾、scoreboard 两条 TODO 与实际检查落点矛盾 | CLAUDE.md §2（注释用简体中文、与代码同源维护）、§4.2（证据规则——对外陈述须与事实一致）、§7（文档守卫）。注：docs-check 只守 doc/ 下的文档一致性，**不覆盖源码注释与交付状态的失步**，这是守卫体系的已知盲区 | 待处置：派 DV 订正措辞——(1) 四处「M3 尚未交付」改为陈述 m3_stub 的真实用途（M1 单元级通路隔离，避免 M1 场景依赖真实 M3），并注明 M3 已于 0.2.2 交付、集成通路走 ppa_top；(2) scoreboard 的两条 TODO 与文件头注释按「已选架构」如实改写，指明 CSR/结果比对的实际落点（自检序列 + ppa_core_driver 内建参考模型 + ppa_ref_model），或补齐实现（二选一由 orch 定 scope）；(3) `tb/uvm/apb_agent/apb_sequencer.sv:1` 的「占位以便将来加仲裁」属正常留白，不在订正范围。**纯注释改动不得触碰任何可综合/可仿真语义**，改完须复跑 `make regress` 确认 32/32 零回归。**rev 复验关单（关单人=rev≠修复人=DV/orch，2026-07-26，被审 HEAD 6a9d934）**：① 订正措辞的九项主张逐条独立核实，**全部属实**——packet_proc_core 交付 commit 经 `git log --diff-filter=A` 实为 b4fb27e（0.2.2）、rtl.f 所列四模块在 tb_top L51/L89/L111/L131 四处例化齐全（三条通路陈述成立）、scoreboard 新注释里的 m1_seq_lib 31 处 uvm_error 比对点 / m3_seq_lib chk_eq 定义 1+调用 14 / m4_seq_lib 调用 8 / ppa_core_driver chk+chkv 共 8 个调用点 / tb-sva 17 条断言 / golden_calc 零调用者，实数复核逐项吻合；附带认定 DV 在处置中推翻了本条登记里 orch 给的 36/33/24 三个错数与「ppa_ref_model 是实际检查落点」的误判并另开 BUG-016，**处置比任务卡更准确**。② 纯注释零语义变更由**去注释机械等价证明**独立确认：5 个改动文件取 a039413^ 与 a039413 两版，去行注释/块注释（含字符串字面量保护）并规范化空白后逐字等同，结论不依赖回归 PASS 背书；本实例另独立实跑 `make regress` 得 32/32 PASS、result_summary.txt 与入库版本逐字一致。③ 新守卫双向验证：`git archive a039413^` 重建修复前树复跑 scan_source_markers 上报 **9 处**（与修复人自述一致），当前树 **0 处**；自建 9 条应放行样例（在途承诺 M5、场景 ID M1-06/M4-02b、「占位以便将来加仲裁」、开放式 TODO、含「补齐」的陈述句、已交付陈述等）**0 误伤**，3 条应报样例全部命中。**关单**。残余边界（不阻断关单，已转 orch 另开加固项）：守卫**召回很窄**——本实例构造的 7 条真过期承诺（用模块名不用 M 编号、句号切断绑定窗口、「尚未补齐」不在动词表、间隔超 24 字符、英文措辞、跨两行折行、「M3 待定」）**全部逃逸**，其中「间隔超 24 字符」与「跨行折行」距原始缺陷文本仅一步之遥；且 sim/flist/rtl.f 那类措辞天然在守卫射程外（本次靠人工查出）。故「report-check 通过」只等于「不存在 BUG-013 那几种字面形态的过期承诺」，**不得**对外表述为「仓库无过期承诺」。审查明细见 doc/evidence/v0.5.3/review-bug-013-014.md §B | a039413 | CLOSED | doc/evidence/v0.5.3/review-bug-013-014.md |
| BUG-014 | 2026-07-26 | 0.5.1 | infra（证据链完整性漏洞——回归判定盲区，影响"防验证造假"纪律根基） | **SVA 断言失败不会让回归变红。** 全仓 49 条断言（DE 侧 rtl/ 32 条 + DV 侧 tb/sva/ 17 条）动作块**一律为 `else $error(...)`**，而 `scripts/regress.py:19-26` 的 `parse_log()` 判 PASS 的唯一依据是 UVM report summary 的 `UVM_ERROR==0 && UVM_FATAL==0`；`$error` 是 SystemVerilog 系统任务、不经 UVM report server，**不计入 UVM_ERROR**，VCS 默认也不因此改 simv 退出码。`scripts/evidence.py:40-42` 同款判定，故断言失败的 log 还能通过 `make evidence` 登记成 ✅。后果：① "make regress 100% PASS" 只覆盖 UVM 侧检查，对 49 条断言零拦截力；② "ASSERT 覆盖率 100%" 只证明断言被**触发过**，不证明断言**成立**，两者组合起来的说服力被显著削弱；③ 任何 ✅ 都可能掩盖一次断言失败。这直接抵触 CLAUDE.md §4.2「没有仿真 log 就没有 ✅」所要保障的证据可信度 | rev 在复核豁免 #12 时做受控负向实验意外证实：剥除 `tb/sva/packet_proc_core_sva.sv` 的 7 处 `disable iff (rst)` 后复跑 `make -C sim run TEST=ppa_m2_09_reset_test SEED=1`，log 中 `"../tb/sva/packet_proc_core_sva.sv", 45: ...a_done_hold: started at 155000ps failed at 165000ps  Offending 'done_o'` 真实失败，**同一份 log 的 UVM report summary 仍是 `UVM_ERROR : 0`、simv 退出码 0**，`regress.py` 判 PASS。发现人=rev（复核豁免 #12 实例），2026-07-26；orch 独立复核：`grep -n -A3 "assert property" tb/sva/*.sv rtl/*.sv` 确认 49 条全部为 `else $error(...)`，`sed -n '19,26p' scripts/regress.py` 确认判定条件 | CLAUDE.md §4.2（证据规则/防验证造假：没有仿真 log 就没有 ✅、证据一律机械生成）、§7（CI 硬门禁：make regress 100% PASS 是里程碑完成的必要条件）；spec §0 适配 7（SVA 纳入验证与覆盖率口径——既纳入口径就必须纳入拦截） | 待处置：派 DV ① 让 `regress.py` / `evidence.py` 在 UVM_ERROR/FATAL 之外**同时**扫描 SVA 失败特征（VCS 断言失败行形态），任一命中即判 FAIL / 拒登证据；② **对全部既有归档证据 log 与本轮重跑的 32 条回归 log 做一次历史回扫**，确认历史上是否真有被漏判的断言失败——若有，逐条登记为新缺陷，历史 ✅ 需重估；若无，则历史结论不受影响、本次仅为堵漏；③ 复跑 `make regress` 用新判定确认 32/32 仍成立。**在本条闭环之前，对外材料不得就"断言"做任何强度声明**（可写"49 条断言，ASSERT 覆盖率 100%"这类覆盖事实，不得写或暗示"断言全部通过/回归拦得住断言失败"）。**rev 复验关单（关单人=rev≠修复人=DV/orch，2026-07-26，被审 HEAD 6a9d934）**：不复用修复人的破坏方式——自建**仓库外**的 bind 负向对照（两条明知为假的断言，分别为「带 else $error」与「**无动作块**」两种形态，经 flist 注入编译，仓库源码在实验期间字节不变，事后 sha256 校验还原）。① **缺陷属实**：注入后 ppa_m2_04_test 打出 2 条断言 4 次失败，同一份 log 的 UVM_ERROR=0 / UVM_FATAL=0 / simv 与 make 退出码均为 0，修复前的单腿判定必判 PASS；无动作块那条同样打出引擎失败行，「判定与动作块无关」成立。② **新判定实测**：regress.py 判 `FAIL ... [SVA失败: 2 条断言/4 次]` 且**退出码 1**（同一次编译内的 ppa_m1_01_test 因不驱动 core 进 PROCESS 仍判 PASS，构成自带阴性对照，说明不是「注入即全红」）；evidence.py 打印失败断言明细并拒登、**退出码 1**、不产生任何证据文件。③ **fail-closed 实测变严格**：去掉 -assert verbose 后，有失败的 log 仍被层 1 引擎行拦下判 FAIL，干净的 log 因缺汇总行判 FAIL、evidence.py 拒登退出码 1。④ **误伤 0**：自建三类语料（33 份本机实跑 log、真跑出来的 VCS `Error-[SE]` 编译诊断 log、13 行对抗样例含 succeeded at 近似形态与被前缀顶开的引用形态）全判 CLEAN。⑤ **历史回扫方法学成立**：其前提经本实例独立坐实——33 份归档摘录中含任何断言信息的为 **0** 份，修复前 KEY_LINE_RE 只认 pass/match/compare ok/check ok，断言引擎行在设计上必被丢弃，故「扫摘录得 0 漏判」信息量确为零，修复人对 orch 方法的证伪成立；替代方法选树无歧义（抽查 4 份证据的 git 历史均为 1 条 commit、从未被改过）；阳性对照（attempts 非零）必要且到位——它排除的正是本实例做出来的退化场景；保真度对照被本实例**加强**为归档摘录「关键检查行」逐行逐字比对后仍全中。**自抽 5 份跨 M1/M1/M2/M3/M4 独立重放（v0.1.6/M1-05、v0.1.7/M1-08、v0.2.3/M2-04、v0.3.0/M3-03、v0.4.0/M4-02d）：全部 0 断言失败、attempts 非零（23/23/39/88/88）、UVM severity 与关键检查行对照全中**，与「0 漏判」结论一致。⑥ 新判定下本实例独立实跑 `make regress` 得 **32/32 PASS**，32 份 log 汇总行一律 `91 assertions, 88 with attempts, 0 with failures`，result_summary.txt 与入库版本逐字一致。修复人自述的残余边界（重放验证的是当年源码在当年 TEST+SEED 下的断言行为、非当年那次仿真进程的字节级重演）**认可**，另补两条前提：证据须产自同 commit 的树（靠 /closeout 自包含提交的流程约定保证，不是重放本身证明的）、工具链未变（同机同 VCS O-2018.09-SP2）。**关单**。同时登记三条**本实例新构造出的漏报向量**（属新机制自身的加固项、非本缺陷残留，已转 orch 另开条目）：R1 判定只看 failures、**不校验 attempts/总数基线**，`$assertoff` 或把 tb/sva 摘出 flist 即可「真违例、判 CLEAN、回归全绿」（实测 `Summary: 2 assertions, 0 with attempts, 0 with failures` 判 CLEAN 退出码 0）；R2 层 1/1b 正则的 hier 段对含 `::` 的类作用域断言**结构性失明**（实测层 1/1b 命中 0、仅层 2 兜住），故 svacheck.py 文件头「层 1 不依赖任何编译选项、fail-closed」对该类不成立，须一并订正；R3 SUMMARY_RE 取最后一条，拼接 log「先失败后干净」判 CLEAN，建议取 max。另记一条**诚实性问题**：commit 461aebc 与 doc/log.md 称「32 份 log 遍布这些信号名故一条未被误伤」**与实测相反**（length_error/type_error/chk_error 在 32 份回归 log 与 33 份归档摘录中出现均为 0 次、UVM_HIGH 下亦为 0，ERROR_STATE 在本仓库根本不存在），误伤结论本身已由本实例自建语料重新立住，但那句支撑话不得进对外材料；「回扫 261 份 log」因清单未落盘不可审计，同样不得引用。审查明细见 doc/evidence/v0.5.3/review-bug-013-014.md §A | 461aebc | CLOSED | doc/evidence/v0.5.3/review-bug-013-014.md |
| BUG-015 | 2026-07-26 | 0.5.1 | infra（源码注释与缺陷/spec 状态失步，BUG-013 同类但在 rtl/ 侧） | `rtl/apb_slave_if.sv:9` 注释写「已知遗留问题（BUG-004，**OPEN**，待 rev/arch 裁决）：…本模块对 PKT_MEM 地址范围的读访问采用 PSLVERR=0 + PRDATA 固定输出 0 的**临时处理，不作为对外行为承诺**」。实际：BUG-004 状态是 **SPEC_CHANGED**（已归档至 bugs-archive.md），spec **r7（2026-07-09）**已把该行为收窄为**正式契约**「APB 读 PKT_MEM：PSLVERR=0 + PRDATA 返回占位 32'h0」。故该注释错两处——缺陷状态过期、且把已成 spec 契约的行为说成"临时处理、不作为对外行为承诺"，而 testplan 的 M1-06/M4-02b 正是按该契约在考核它。BUG-013 新增的源码注释守卫**抓不到这一类**（它绑定的是里程碑编号，不是缺陷号） | `git grep -n "BUG-004" rtl/`（静态）；对照 `grep -n "BUG-004" doc/bugs-archive.md` 的状态列与 `grep -n "r7" doc/spec.md` 的修改记录。发现人=DV（BUG-013 处置实例，超出其任务卡范围故只列清单未擅改），2026-07-26 | CLAUDE.md §2（注释与代码同源维护）、§8（单一事实源：行为主张须引 spec 章节且与现行 spec 一致）；spec 修改记录 r7 | 待处置：派 DE（rtl/ 归 DE）订正该注释为陈述现行契约并引 spec §6.3(r7)；可选顺带扩展 BUG-013 新增的守卫——增加「源码注释引用的 BUG-ID ⇄ bugs.md/bugs-archive.md 状态位」交叉校验，堵掉这一类。**rev 复验关单（关单人=rev≠修复人=DE，2026-07-27，被审 HEAD 7616e35）**：① 订正注释九项主张逐条指回权威源全部属实——spec §6.3 r7(L399)/r7 修改记录(L14)/§2.3 M2 表注(L212)、BUG-004 归档状态=SPEC_CHANGED、testplan M1-06(L19)/M4-02b(L61) 均在列且引 r7，旧注释"OPEN/临时处理/不作为对外行为承诺"两处错误均被订正；② 纯注释零语义由**去注释机械等价**独立证明（2a7d17d^ 与 2a7d17d 两版去行/块注释+规范化空白后 196 行逐字等同），不依赖回归背书。审查明细见 doc/evidence/v0.5.5/review-bug-015-016-017.md §A。**关单** | 2a7d17d | CLOSED | doc/evidence/v0.5.5/review-bug-015-016-017.md |
| BUG-011 | 2026-07-16 | 0.4.1 | infra（scripts/docs.py 里程碑门禁，非 spec 歧义） | `make next` 里程碑三条硬条件核对中"rev 签核记录存在"检查恒真：`cmd_next()` 的 `any(d.glob(f"review-M{mnum}*.md") for d in ev_dirs)` 把 `Path.glob()` 返回的生成器对象本身当元素传入 `any()`——生成器对象恒为真值，只要 ev_dirs 非空即判"已签核"；且模式大写 `review-M{mnum}` 与既有命名约定 `review-m2-milestone.md`/`review-m3-milestone.md`（小写 m）不匹配，即使修好 any 判定也永远匹配不到。后果：M4 场景全 ✅ 后 `make next` 误报"三条硬条件已齐 → bump-minor + tag"，而 doc/evidence/v0.4.*/ 下实际不存在任何 review-m4 签核记录——门禁放行未签核里程碑，违反 CLAUDE.md §4.1 硬条件③ | `make next`（0.4.1 工作区，M4 场景全 ✅ 且无 review-m4*.md 时输出"M4 三条硬条件已齐"即复现；修复后同条件应输出"还差：rev 里程碑签核"）。发现人=DV（M4-01/03/05 收口实例），2026-07-16 本地实测；静态缺陷，无需仿真 | CLAUDE.md §4.1（M 完成判据三条硬条件③ rev 审查记录存 doc/evidence/）、§7（docs.py 属质量门禁） | orch 修复（2026-07-16，门禁脚本属 orch 维护的守卫系统，不涉 rtl/tb）：① 内层改真实匹配判定 `any(any(d.glob(...)) for d in ev_dirs)`（内层 any 消费生成器、按是否匹配到 Path 判真）；② 模式改小写 `review-m{mnum}*.md` 对齐 v0.2.3/v0.3.0 既有命名约定，命名规范定为 `review-m<N>-milestone.md`（小写）。复验（关单人≠修复人）：交 M4 里程碑签核 rev 实例——签核记录落盘前复跑 `make next` 应报"还差 rev 里程碑签核"，落盘后复跑应报"三条硬条件已齐"，两态都对才关单。**rev 复验关单（关单人=rev≠修复人=orch，2026-07-16）**：读 scripts/docs.py:505-520 确认修复正确——内层 `any(any(d.glob(f"review-m{mnum}*.md")) for d in ev_dirs)`（内层 any 消费生成器按真实匹配判真）+ 小写模式 `review-m{mnum}*.md` 对齐既有命名；态1（签核记录落盘前）`make next` 实报「M4 条目全 ✅，还差：rev 里程碑签核（review-m4-milestone.md）」，态2（本记录 doc/evidence/v0.4.1/review-m4-milestone.md 落盘后）`make next` 实报「M4 三条硬条件已齐 → make bump-minor + git tag v0.5.0」，两态均符合预期；且三条硬条件经 rev 独立复算为真（回归 32/32 复现、六类覆盖率 urg 逐类复现一致）——关单。 | 5a58c64 | CLOSED | doc/evidence/v0.4.1/review-m4-milestone.md |
| BUG-012 | 2026-07-26 | 0.5.0 | infra（lint 门禁登记遗漏，非 RTL/spec 缺陷） | HEAD 处 `make -C sim lint` 实测本仓库范围内（`../rtl/` `../tb/`）共 84 处告警，而 `doc/lint-waivers.md` + 归档件的 11 条豁免仅覆盖 74 处，**10 处从未登记**：`tb/sva/packet_proc_core_sva.sv:24,29,34,40,45,51,56`（7 处 Lint-[SVA-DIU]）、`tb/uvm/test/ppa_m2_01_test.sv:31` 与 `tb/uvm/test/ppa_m2_02_test.sv:27,42`（3 处 Lint-[WMIA-L]）。违反 CLAUDE.md §7「lint 门禁：DE 交付条件——`make lint` 干净，或告警登记 `doc/lint-waivers.md` 经 rev 复核」。在补齐并经 rev 复核之前，对外展示材料不得出现任何「lint 告警全部登记/清零/干净」类表述 | `make -C sim lint`（本地 VM，需先 export VCS_HOME/VERDI_HOME/LM_LICENSE_FILE；见到本仓库范围告警即 exit 1 属预期，非跑挂），对 `sim/out/lint.log` 按 (类别, 文件, 行号) 去重统计得 84 处；与 `doc/lint-waivers.md`+`doc/lint-waivers-archive.md` 对象列逐条对账，差集即上述 10 处。发现人=rev（R2 审查 scripts/report.py 抽数正确性时，为验证 sites_total=74 是否仍成立而实跑 lint，附带发现），2026-07-26；静态缺陷，无需 UVM 仿真 | CLAUDE.md §7（质量门禁-lint 门禁）；spec §0 适配 8（lint 门禁为 DE 交付条件） | DV 处置（2026-07-26，登记人=DV，独立实跑 `make -C sim lint` 复现 84 处并自算差集，与本条所列 10 处完全一致）：① **7 处 Lint-[SVA-DIU]**（tb/sva/packet_proc_core_sva.sv）核对源码确认与已批准的 #3/#4 同写法同根因（单一信号 rst + `disable iff`，bind SVA 的等价必要用法）→ 登记豁免 **#12**，复核栏留「待 rev 复核」，未自行批准。② **3 处 Lint-[WMIA-L]**（ppa_m2_01_test.sv:31、ppa_m2_02_test.sv:27,42）核对后判定根因与 #6（UVM 库宏内部实现、不可控）**不同**——此处是 `foreach` 循环 `int` 下标隐式赋给 `bit[7:0] payload[]` 的宽度不匹配，仓库内 ppa_m2_08_rand_test.sv / ppa_m2_09_reset_test.sv / m4_seq_lib.sv 已有 `8'(...)` 显式截断的既定写法可低风险照抄 → **不登记豁免，直接改源码修复**（`i + 1` → `8'(i + 1)`、`i` → `8'(i)`，数值范围内无行为变化），沿用本项目 Lint-[ULCO] 6 处直接改源码不豁免的先例（BUG-006）。③ 附带订正两处登记表笔误（不改处数、不影响既有结论）：#8 对象列行号由 BUG-009 修复前的 278…322 订正为实测 282,287,292,297,302,308,313,321,326；撤销 #11 复核栏「ppa_core_driver 记 29 实为 L30」的错误注记——读源码确认 L29 是 `wait (vif.rst_n === 1'b1);`、L30 是 `@(vif.drv_cb);`，两处独立告警且实测均在告警列表中，原登记 10 处无误。**验证**：修复后 `make -C sim lint` 复测本仓库范围 81 处，逐一核对均已被 #1~#12 覆盖；`make -C sim regress` **32/32 PASS**（含改动的 ppa_m2_01/02_test）确认零回归；`docs.py --check` 与 `report.py --check` 均通过、`sites_line_drift` 归零。**待复验关单**：交 rev 实例复核豁免 #12 合法性 + 复算 lint 对账（关单人 ≠ 登记人）。**rev 复验关单（关单人=rev≠修复人/登记人=DV，2026-07-26）**：不采信条目自述数字，独立实跑 `make -C sim lint` 并自实现 (类别,文件,行号) 去重统计——HEAD 处本仓库范围实测 **81** 处（NS 32 / SVA-DIU 45 / WMIA-L 4），与本表+归档件 #1~#12 登记的 81 处**双向差集为空**（无未登记项、无幽灵登记）；`report.py --json` 从登记表侧独立解析出 `sites_total=81`，两条互不依赖的取数路径同时命中，对账无误。**「修复前 84 处」经受控负向复现证实**：cp 备份 + `git show 615f31a^:` 取出修复前两文件覆盖（全程零 git 写操作，还原后 `sha256sum -c` 均「成功」）、独立 `OUT=out_neg` 跑 lint 得 **84** 处，集合差恰为 `WMIA-L @ ppa_m2_01_test.sv:31 / ppa_m2_02_test.sv:27 / :42` 三处，后集无任何独有项——修复精确消除该 3 处、无新增、无行号漂移副作用。**逐项裁决**：① 豁免 #12 **批准**（同 #3/#4 写法根因；负向实验剥除 7 处 `disable iff` 后 `a_done_hold` 在 DONE 态复位注入处产生虚假失败，证其语义承重、非「图省事走豁免」，详见审查记录 §2）；② 3 处 WMIA-L 直接改源码**认可**——根因与 #6 确不同（#6 报在 UVM 库宏内部 `__m_uvm_status_container.status = 1;` 调用点不可控，本处报在测试自身语句 `n3.payload[i] = (i + 1);` 可在调用点修），且**行为等价经独立值域证明**（`payload` 为 `bit [7:0] payload[]`，三处紧邻上一行均 `new[28]` 故 `i∈[0,27]`，RHS 值域 [1,28]/[0,27]/[0,27] 全 ⊆[0,255]，`8'()` 不截有效位、bit[7] 恒 0 无符号扩展交互，且这三个 directed item 无 randomize 覆写、期望值由同一数组经 `predict()` 现算）——该结论不依赖回归 PASS；③ #8 行号订正**成立**（实测 282,287,292,297,302,308,313,321,326 与订正后逐个吻合、订正前无一命中，`report.py` 的 `sites_line_drift` 已归零）；④ #11 注记撤销**成立**（`ppa_core_driver.sv` L29=`wait (vif.rst_n === 1'b1);`、L30=`@(vif.drv_cb);` 两条独立语句，实测 NS 告警 29 与 30 并存，原登记 10 处无误）。机械层：`reviewed` 11/12→**12/12**、`pending_review` `['12']`→**清空**、`all_reviewed`→**true**，`report.py --check` 通过（warn 7→6，消失的正是「豁免 #12 尚未经 rev 复核批准」）、`make docs-check` 通过。**关单**。另向 orch 提交一项须另行登记的新发现（本次负向实验意外证实）：SVA `$error` 失败不计入 UVM_ERROR 且 simv 退出码为 0，而 `regress.py:15-26` / `evidence.py:41-42` 只看 UVM_ERROR/FATAL，故全部 49 条断言目前「只报不拦」——不影响历史结论（功能缺陷均由 UVM 侧参考模型比对捕获），但削弱「回归 100% PASS + 断言覆盖率 ≥90%」的组合说服力，详见审查记录 §7 R1 | 615f31a | CLOSED | doc/evidence/v0.5.1/review-lint-waiver-12.md |
| BUG-008 | 2026-07-13 | 0.2.2 | spec 歧义 | CFG.algo_mode / CFG.type_mask / PKT_LEN_EXP.exp_pkt_len 三者均为 RW（§5.2），§6.3 访问限制表只显式约束 PKT_MEM 在 busy=1 时写入被拒（PSLVERR=1），未见对这三个 CSR 施加同等的 busy 期间写保护或"取样点"约定；§7.2 状态转移表列出 IDLE→PROCESS 转移的显式动作（置 busy_o=1、清除上一帧结果、初始化字计数器、从 addr=0 开始读 M2），§7.3 第 0 拍数据流描述读 Word0 同拍提取头字段并消费这三个 CSR 做三类判定，但两处均未说明这三个 CSR 是否需在 start_i 被接受的那一拍做快照/锁存，还是允许 busy 期间/多帧之间被 APB 实时改写、判定时直接取"当拍活值"。DE 交付 packet_proc_core 时选择后者（三个输入直接组合接入第 0 拍判定逻辑，无锁存寄存器），design-prompt 已将此列为唯一未决项、要求另行提案，未在 RTL/design-prompt 内私自敲定 | 静态歧义，无需仿真（DE 交付 packet_proc_core 时自评发现；doc/design-prompt/packet_proc_core.md:87,110 已登记"未决"；0.2.2 log.md 交接同步指出，DV 编写相关 checker 前需先厘清） | §5.2（CFG/PKT_LEN_EXP 字段属性）§6.3（PKT_MEM 的 busy 写保护，对照 CFG/PKT_LEN_EXP 无同等约束）§7.2（IDLE→PROCESS 显式动作清单，未列 CSR 快照动作）§7.3（第 0 拍消费三个 CSR 做判定） | rev 裁决（2026-07-13）：取方向 (B)——实时取值 / 组合直取，M3 不对 algo_mode/type_mask/exp_pkt_len 做 start 时刻快照锁存；同时补 spec 澄清注，将"软件须在 start 被接受前配置好这三个 CSR 且在整个 busy 期间保持不变"的软件契约写进 spec，明文 M3 判定取当拍活值、不保证 busy 期间改写原子生效。**RTL 无需返工**（DE 当前组合直取、无锁存即与本方向一致）。依据：(1) §7.2 IDLE→PROCESS 动作清单显式穷举四项（置 busy_o=1；清除上一帧结果；初始化字计数器；从 addr=0 读 M2），未列"快照 CSR"；沿用 BUG-003/004 确立的"显式清单/具体条文优先"原则——连"清除上一帧结果""初始化字计数器"这类内部动作都逐条列出，若要求快照配置必同列，缺此项即不要求快照。(2) §5.1/§5.2：CFG（algo_mode/type_mask）、PKT_LEN_EXP（exp_pkt_len）均为纯 RW，定义仅"可读可写、写入后保持新值"，无"start 采样""影子寄存器"时序限定；对比同表 CTRL.start 为 W1P 且明写"仅 enable=1 && busy=0 时被接受"，作者在需要时明确标注时序语义，这三个 CSR 的沉默是有意，不能反推快照。(3) §6.3/§8.3 的 busy 写保护只施于 PKT_MEM 并给出理由（"M3 正在读取，禁止修改"），CFG/PKT_LEN_EXP 无同等条文——作者已就"busy 期间被改写"为数据通路立法却未延伸到配置，据"明示其一即排除其余"，配置系有意留作 busy 期间可写；此条同时否证方向 (C)：若欲禁止 busy 改配置，应如 PKT_MEM 写进 §6.3/§8.3，现状明确没有。(4) §11.4 规范驱动序列"写 packet→配置 CTRL→start→轮询 done→读结果"是 spec 认可的软件流：配置在 start 前完成、busy 期间只轮询不改写；在此使用模型下"实时取值"与"快照"结果恒等。(5) §10.1/10.2/10.3（N-1..N-4/E-1..E-6/B-1..B-4）与 §11.3 Lab2 必做1-3+选做4-5 无任一场景在 busy 期间改写 CFG/PKT_LEN_EXP，均在 start 前置好并整帧稳定，故快照与实时取值对 M2 验收行为不可区分、无功能价值（沿用 BUG-004"对验收无功能价值"论证）；既无验收价值应取最小改动、spec 沉默默认读法，新增影子寄存器纯属镀金、徒增隐藏状态与覆盖率负担。(6) 架构自洽：BUG-003/r6 已定 M3 第 0 拍组合当拍消费 mem_rd_data_i，配置输入同为 M1 组合广播（§2.3 M1 表 algo_mode_o/type_mask_o/exp_pkt_len_o），同属"当拍活值"类输入；对 rd_data 组合消费却单独给配置加锁存是无依据的架构不对称。**核实 DE 现实现（rtl/packet_proc_core.sv）**：三配置输入直接进组合判定（algo_mode_i 见 L120、type_mask_i 见 L112-115、exp_pkt_len_i 见 L104-109），无锁存寄存器，与方向 (B) 一致；需澄清一处精确取样点——错误标志组合判定（L106-124）虽逻辑上"第 0 拍判定"，其输出置位实在 PROCESS→DONE 那一拍寄存（L247-256，条件 word_cnt_q==last_word_cnt），而头字段已在第 0 拍锁存（L219-224）、配置三输入未锁存，故配置实际取样点=PROCESS→DONE 寄存拍（即最后一读拍）的活值，非严格"第 0 拍活值"；在配置整帧稳定前提下（所有验收场景+§11.4 规范流）此取样点与"第 0 拍值""快照值"恒等、无功能差异，RTL 无需返工，但 spec 澄清注须把稳定窗口精确写成"自 start 起直到 done（整个 busy 期间）保持不变"而非仅"start 前"以覆盖该取样点。**否决方向 (A)（强制快照锁存）**：无 spec 条文支持（§7.2 清单未列、§5.2 属性未标），引入任何 CSR 都读不到的隐藏影子状态，与 §6.3/§8.3 反复申明的教学简洁意图相悖，并制造"CFG 读回值≠M3 实际所用值"的观测不一致，需 DE 返工加 3 个影子寄存器、零收益。**否决方向 (C)（busy 期间对三 CSR 加 PSLVERR 写保护）**：虽惯用法上与 §6.3 对称，但需返工已交付的 M1（apb_slave_if）加 busy 门控（属 M2 之外且波及已回归模块、回归风险+新 testplan 行）、改变可观测 APB 行为（当前成功写改报 PSLVERR=1，是比 A/B 更大的行为变更）、与现 §8.3/§6.3 只保护 PKT_MEM 的条文冲突、且无场景需要；以硬件保护解一个当前无场景触发的问题属过度工程——否决其硬件保护做法，但采纳其合理内核：把"帧内不得改配置"以软件契约（文档）而非硬件写进 spec。**建议 spec 改法（待 orch 应用+pin，应用后置 SPEC_CHANGED，拟为 r10）**：① §5.2 CFG 行、PKT_LEN_EXP 行说明列（或表下统一注）补"algo_mode/type_mask/exp_pkt_len 为帧级配置：硬件在 M3 处理期间组合取当拍活值、不做 start 快照锁存；软件契约：须在 start 被接受前配置好并在整个 busy=1 期间保持不变；busy 期间改写不被 §6.3 禁止（不报 PSLVERR），但对当前帧判定的生效与否为 UNSPECIFIED（取样点为 PROCESS→DONE 寄存拍活值），不保证原子生效（BUG-008 裁决，r10）"；② §7.2 表下补注"IDLE/DONE→PROCESS 不对这三配置做快照，PROCESS 期间组合取活值参与第 0 拍判定、结果于 PROCESS→DONE 寄存（见 §5.2 配置契约注、BUG-008/r10）"；③ §7.3 第 0 拍注补一句指向 §5.2 配置契约，明确 algo_mode 决定 chk_error 是否判定、type_mask 参与 type_error、exp_pkt_len 参与 length_error 均取当拍活值、依赖软件 busy 期间保持配置稳定；④（一致性，可选）§6.3 表或其下教学提示补"CFG/PKT_LEN_EXP 不在 busy 写保护之列（区别于 PKT_MEM），帧内稳定性由软件契约保证（§5.2 注）"以免读者误判遗漏。**受影响清单（供 orch，非强制）**：testplan 建议新增一行（如 M2-07）锁定"配置 start 前置好、busy 期间不改写→结果符稳定配置预期"，并加负向观测"busy 期间写 CFG 不报 PSLVERR"（与 §8.3 一致），使"配置无 busy 写保护"被实测而非默认；design-prompt/packet_proc_core.md L87/L110 的"唯一未决/未决:无"更新为引用 r10（配置取样点已定为实时取值+软件契约）、把"DV 编写 checker 前需另行提案"改为引用 r10；rtl/packet_proc_core.sv 模块头时序契约要点可选补一句"配置三输入组合取活值、不锁存，依赖软件 busy 期间稳定（BUG-008/r10）"（仅注释、非返工，可并入下次触及）；DV 相关 checker/SVA 从 r10 推导（整帧稳定前提下 DONE 拍 length/type/chk/format_ok 与帧配置一致，不得构造 busy 期间改配置的激励去卡 DUT——那是 UNSPECIFIED 区）。**orch 已应用（2026-07-13，spec 修改记录 r10）并重新 pin；testplan 新增 M2-07 锁定该行为**。 | - | SPEC_CHANGED | - |
| BUG-007 | 2026-07-09 | 0.1.6 | infra（sim 构建，非 spec 歧义） | `make -C sim regress` 若在残留 `sim/out` 上运行（例如刚跑过 `make lint`，其 `out/simv_lint` 与 `compile`/`run` 共用 `out/csrc` 等构建产物但 VCS_OPTS 不同），会因构建数据库损坏报 `Error-[VFS_SDB_ERROR]`（constraint.sdb 相关）产生假失败；`make clean` 后重跑即 100% 复现真实结果。rev 里程碑抽查时首跑（脏 out）复现 4/7 假失败，`make clean` 后复跑才 7/7 PASS，暴露回归证据可信度隐患：调用者若不知情先 `make clean`，`make evidence`/`make regress` 生成的证据可能是假失败或（反向）掩盖真失败 | 先 `make -C sim lint`（产生 `out/simv_lint` 等产物）再不清理直接 `make -C sim regress`，2026-07-09 本地 VM rev 实测复现 | CLAUDE.md §4.2（防验证造假：证据必须真实可信） | orch 已修复（2026-07-09）：`scripts/regress.py` 的 `main()` 在回归循环前新增 `subprocess.run(["make", "-C", SIM, "clean"])`，确保每次 `make regress` 从干净状态起跑，不依赖调用者记得先手动 clean。本地 VM 复验：先跑 `make lint` 污染 `out/` 后直接 `make regress`，7/7 PASS（无 BUG-007 描述的假失败），证实修复有效。状态待下一提交回填修复 commit 后置 FIX_READY，待复验关单（关单人≠修复人）。 | 5c9d864 | CLOSED | doc/evidence/v0.1.7/BUG-007.log |
| BUG-010 | 2026-07-14 | 0.3.0 | spec 歧义 | §2.1 顶层框图（ASCII 第 113–115 行、mermaid 第 145/148 行）将 `done_o` 画为 `ppa_top` 对外引脚；§2.3 Top ppa_top 端口表（第 241–253 行，11 信号，本仓库端口唯一权威）**不含 `done_o`**，仅 M3 内部 `done_o` 接 M1 `done_i`。两处对"顶层是否引出 done_o 引脚"直接矛盾 | 静态歧义，无需仿真（arch 撰写 doc/design-prompt/ppa_top.md 时发现） | §2.1（第 102–118 行框图）§2.2（第 158 行）§2.3（Top 表第 241–253 行、M1/M3 表「说明」列） | **rev 裁决（2026-07-14）：取方向 (a)——删除 §2.1 框图中 done_o 对外引脚画法，与 §2.3 Top 端口表对齐；ppa_top 不引出 done_o 顶层引脚，M3.done_o 仅内部接 M1.done_i。RTL/testplan/feature-matrix 无需返工**（arch 临时处理已与本裁决一致）。依据：(1) §8.1 第 474 行**显式将 `done_o` 定名为"M3 内部输出信号"**，用途="驱动 M1 的 STATUS.done 与中断判定"，第 475 行 STATUS.done 与 done_i(=M3.done_o) 直连——spec 权威条文已定义 done_o 的角色为 M1↔M3 内部握手，其唯一消费者是 M1.done_i，无对外观测者，与"顶层对外引脚"定位矛盾；§2.1 框图属渲染示意，遇条文/表冲突以条文/表为准（沿用本 spec §7.1 下"以 7.2 表为准"及 BUG-003/004/008 确立的"显式条文/表优先于图示"原则）。(2) §2.3「Top ppa_top」表为本仓库端口唯一权威（design-prompt README + CLAUDE.md §8 修改路径唯一），11 信号不含 done_o；irq_o 之所以在表内是因其为真正的对外中断输出（来自 M1），done_o 无同类对外消费者。(3) §2.2 第 158 行 ppa_top="引脚透传·无额外状态逻辑"：done_o 引脚化对功能零增益。(4) 全部 M3 验收场景（M3-01/02/03、§11.4 必1-3、附录 A）均经 APB 读 STATUS.done 观察完成，无一依赖顶层 done_o 引脚——方向 (b) 将新增一根无 spec 消费者、无场景需要的顶层引脚，并波及 design-prompt/testplan/feature-matrix，属镀金（沿用 BUG-004/008 "对验收无功能价值即取最小改动"）。**否决方向 (b)**：与 §8.1 "内部输出信号"定名冲突、零验收价值、徒增改动面。**建议 spec 改法（供 orch 应用+pin，拟为下一 spec 修改记录版次 r11，应用后本 bug 置 SPEC_CHANGED）**：① §2.1 ASCII 框图删去 done_o 对外引脚分支（第 113 行 `done_O`、第 114–115 行连线与"(顶层对外引脚)"分组中的 done 支路），仅保留 irq_o 为唯一顶层对外引脚；done_o 的 M3→M1 内部路径已由第 105 行"状态结果回传(M1<->M3)"、第 106–107 行互连覆盖，无需另画。② §2.1 mermaid 删去 `DONE["done_o"]` 节点（第 131 行）及 `M3 --> DONE`（第 145 行）、`DONE --- EXT`（第 148 行）两条边，保留 IRQ 支路。③ 建议在 §2.1 框图下补一句注："M3.done_o 为内部信号，接 M1.done_i 驱动 STATUS.done 与中断（§8.1），不引出顶层；软件经 APB 轮询 STATUS.done 观察完成（BUG-010 裁决，r11）"。④ 受影响清单：design-prompt/ppa_top.md 第 41/124 行"待仲裁-A/未决"更新为引用 r11（已裁决方向 a）；testplan/feature-matrix/RTL 无需改动。 **orch 已应用（2026-07-14，spec 修改记录 r11）并重新 pin；design-prompt/ppa_top.md 对应引用已同步更新为 r11。** | - | SPEC_CHANGED | - |
| BUG-009 | 2026-07-14 | 0.2.2 | **RTL（rev 裁决 2026-07-14：方向A/大端为准，判 RTL bug，归 DE 修 L75-78/L141-144 bit 切片；仲裁记录 doc/evidence/v0.2.3/review-bug-009-arbitration.md）** | packet_proc_core 包头四字节 bit 位映射与 spec 附录 A/B 相反：pkt_len 取 mem_rd_data_i[7:0]、hdr_chk 取 [31:24]（小端），spec 附录钉为 Byte0(pkt_len)→[31:24]（大端）。N-1 包 Word0=32'h04010005 期望 res_pkt_len=4/res_pkt_type=0x01/type_error=0/format_ok=1，实测 res_pkt_len=0x05/res_pkt_type=0x00/type_error=1/format_ok=0。同一根因致 M2-01~M2-07 全 FAIL。详情 [doc/bugs/BUG-009.md](bugs/BUG-009.md) | `make run TEST=ppa_m2_01_test SEED=1` | 附录A(spec.md:826)/B.1(866)/B.2(874)/B.3(882) 数值示例钉大端；§6.1 §3.1 字节顺序 | 根因：`rtl/packet_proc_core.sv` 头字段抽取（L75-78）与 payload 字节抽取（L141-144）按小端切片（pkt_len=[7:0]…hdr_chk=[31:24]、byte0=[7:0]…byte3=[31:24]），与 spec 附录 A/B 大端约定（Byte0→[31:24]…Byte3→[7:0]）相反。修复：两处 bit 切片改为大端——头字段 pkt_len_eff=[31:24]/pkt_type_eff=[23:16]/flags_eff=[15:8]/hdr_chk_eff=[7:0]；payload byte0_cur=[31:24]/byte1_cur=[23:16]/byte2_cur=[15:8]/byte3_cur=[7:0]。仅改端序切片，不动 FSM/时序/判定逻辑，无新行为。本地 VM 真跑：`make -C sim compile` 0 error/0 warning，`make -C sim lint` 干净（packet_proc_core 上仅既有已批准豁免 #8 的 SVA-DIU，未引入新告警）。**DV 复验驳回（2026-07-14，FIX_READY→OPEN，关单人≠修复人）：9c28fea 修复不完整——只改了组合头字段抽取 L76-79 与 payload L143-146（大端，正确），遗漏头字段锁存 `always_ff` L222-225（仍小端 pkt_len_q<=[7:0]…hdr_chk_q<=[31:24]）。致组合 *_eff（大端）与锁存 *_q（小端）不一致：res_* 在 PROCESS→DONE 拍写入，单字帧该拍 word_cnt_q==0 取大端 eff（N-1/M2-04/05 PASS），多字帧 word_cnt_q≠0 退回小端锁存 *_q（M2-01/02/03/06/07 FAIL，res_pkt_len=hdr_chk 字节/res_pkt_type=flags 字节）。最小复现 `make run TEST=ppa_m2_01_test SEED=1` 仍 FAIL(UVM_ERROR=9)；`make -C sim regress` 12/17（M1 9/9+smoke PASS 无回归，M2 04/05 PASS）。建议修复：L222-225 锁存改大端（pkt_len_q<=[31:24]…hdr_chk_q<=[7:0]）。TB 参考模型/激励经复审为 spec 附录大端、无误未改。详情见 [doc/bugs/BUG-009.md](bugs/BUG-009.md) 文末"复验结果"。**第二次修复（DE，2026-07-14，≠首次修复人）**：将头字段锁存 `always_ff` 四行改大端（pkt_len_q<=[31:24]/pkt_type_q<=[23:16]/flags_q<=[15:8]/hdr_chk_q<=[7:0]），与组合 `*_eff`（L76-79）同端序，多字帧 DONE 拍退回锁存值时不再取错字节。独立核实 payload 路径无同类"组合+锁存"镜像——`byte*_cur`（组合大端）直接馈入 `sum_next`/`xor_next` 增量累加进 `sum_acc_q`/`xor_acc_q`，累加器存的是大端解析后算术中间值而非原始字节镜像，无小端残留，故本缺陷唯一残留点即头字段锁存，无第三处遗漏。仅改端序切片、不动 FSM/时序/判定。本地 VM 真跑：`make -C sim compile` 0 error/0 warning（KDB 0/0）；`make -C sim clean` 后 `make -C sim lint` packet_proc_core 仅 9 条 SVA-DIU（豁免 #8 覆盖），未引入新告警/新类别。未跑 UVM 场景（功能复验属 DV 关单职责）。详情见 [doc/bugs/BUG-009.md](bugs/BUG-009.md) 文末"第二次修复"。 | 9c28fea, b8a1890 | CLOSED | doc/evidence/v0.2.3/BUG-009.log |
| BUG-006 | 2026-07-09 | 0.1.5 | infra（tb/ 遗留 lint 告警，非本轮 DE 引入） | `make lint` 判定范围原始实现对整个 `+lint=all` 输出做 `grep -q "Lint-"`，把供应商 UVM-1.2 库头文件（`$VCS_HOME/etc/uvm-1.2/*.svh`，与本仓库无关、DE/DV 无法修复）也计入失败判据，导致该 gate 事实上永远无法通过；BUG-005 修复后 `make lint` 首次真正跑到全量扫描，暴露此设计缺陷，同时暴露仓库自身 tb/ 范围内 17 条**本轮之前**（0.1.0 脚手架）就存在的告警：`tb/tb_top.sv:20`、`tb/uvm/apb_agent/apb_driver.sv:20/21/46/49`、`tb/uvm/apb_agent/apb_monitor.sv:23`（均 `Lint-[NS] Null statement`，`@(...)`/`wait(...)`/`repeat(...)@(...)` 类同步语句的常见 VCS 误报）；`tb/uvm/apb_agent/apb_seq_item.sv:10-13`（`Lint-[WMIA-L]`，uvm_field 宏展开内部赋值，常见宏展开误报）；`tb/uvm/env/ppa_ref_model.sv:47/68/70`（`Lint-[ULCO]`，8-bit `pkt_len` 与 32-bit `PKT_LEN_MIN/MAX`/6-bit `exp_pkt_len` 比较位宽不等，比较运算按标准位扩展无截断风险，但建议 DV 确认非隐藏 bug）。两个新交付的 rtl/ 模块（apb_slave_if.sv、packet_sram.sv）范围内仅 13 条 `Lint-[SVA-DIU]`，DE 已登记 `doc/lint-waivers.md` 待 rev 复核，不在本条范围内 | `make -C sim lint`（FLISTS 已按 BUG-005 修复顺序），2026-07-09 本地 VM 实测，完整 log 见 `sim/out/lint.log` | CLAUDE.md §7（lint 门禁为 DE 交付条件）；本条涉及 tb/，非 DE 职责范围 | orch 已止血：`sim/Makefile` lint 判定范围收窄为仅 `../rtl/` `../tb/`（排除供应商库路径），使 gate 恢复可用性，此为纯脚本判定范围修正、不改变任何设计/验证语义。**DV 已处置（2026-07-09）**：① `Lint-[NS]`（tb_top.sv:20、apb_driver.sv:20/21/46/49、apb_monitor.sv:23，共 6 处）判定为标准 UVM 同步等待写法，登记豁免 `doc/lint-waivers.md` #5（待 rev 复核）；② `Lint-[WMIA-L]`（apb_seq_item.sv:10-13，共 4 处）判定为 `` `uvm_field_int`` 宏内部实现、仓库代码不可控，登记豁免 #6（待 rev 复核）；③ `Lint-[ULCO]`（ppa_ref_model.sv:47/68/70，共 6 处子表达式）判定为可修复告警，已直接加显式 `int'(...)` 类型转换消除（非豁免），orch 复验 `make -C sim regress` 7/7 PASS 无回归。orch 复核时另发现 `tb/uvm/env/m3_stub_driver.sv`（本轮 0.1.6 新交付、非 0.1.0 遗留，8 处同类 `Lint-[NS]`）未登记，已补登记豁免 #7（同一根因，待 rev 复核）。`make -C sim lint`（收窄范围内）复跑仅剩 #1~#7 共 41 条已登记 `SVA-DIU`/`NS`/`WMIA-L`，`ULCO` 已清零，无其他未处置发现。状态待下一提交回填修复 commit 后置 FIX_READY，待复验关单（关单人≠修复人）。 | 5c9d864 | CLOSED | doc/evidence/v0.1.7/BUG-006.log |
| BUG-004 | 2026-07-09 | 0.1.5 | spec 歧义 | §6.3 表第 3 行规定"APB 读 PKT_MEM｜任意时刻｜返回当前 SRAM 内容（不受 busy 保护）"，但 §2.3 M1 apb_slave_if 端口表（第 158–192 行）中 M1 对 PKT_MEM 只有写通路输出（pkt_mem_we_o/addr_o/wdata_o），**没有任何从 M2 读回数据的输入端口**；M2 的 rd_en/rd_addr/rd_data 按 §2.3 M2 表与 §7 仅接到 M3。M1 单模块内无法在 APB 读 0x040~0x05C 时把真实 SRAM 内容送上 PRDATA，与 §6.3 行为要求结构性冲突 | 静态歧义，无需仿真（DE 实现 apb_slave_if 时发现，design-prompt 未提及 PKT_MEM 读回通路，testplan M1-02 也只界定"本模块负责的写通路"，未含读回） | §2.3（M1/M2 端口表）§6.1 §6.3 | 待裁决。DE 侧临时处理（不作为对外行为承诺，仅为使 PSLVERR 语义在无数据通路下保持合法访问不产生副作用）：PADDR 落在 PKT_MEM 窗口 `0x040~0x05C` 时，读访问 PSLVERR=0（§8.3"合法读写"未按方向区分，读不应报错）、PRDATA 固定输出 32'd0（M1 无数据源，非真实 SRAM 内容，仅避免 X 态）；写访问行为不受影响（正常走 pkt_mem_we_o 通路）。请 rev/arch 裁决：(a) 是否需在 §2.3 给 M1 新增 SRAM 读回端口（如 pkt_mem_rd_data_i）并相应改 M2/ppa_top 布线，或 (b) §6.3 该行为收窄为"仅经调试口/不经 APB PRDATA"等其他澄清，或 (c) 确认 PKT_MEM 经 APB 只读回 0 为可接受简化并明文写入 spec。**rev 裁决（2026-07-09）：取方向 (b)+(c) 融合——§6.3 该行收窄，明文 APB 读 PKT_MEM 窗口返回 PSLVERR=0（合法访问，与 §8.3"合法读写 CSR/PKT_MEM"一致）、PRDATA=32'h0 占位值（本仓库架构下 M2 读端口专供 M3，APB 无读回通路，不保证反映 SRAM 真实内容）。否决方向 (a)（新增读回端口）。依据：(1) §2.3 M1/M2 端口表是结构性契约——M1 对 PKT_MEM 只有写通路、M2 读端口仅接 M3（design-prompt/packet_sram.md 端口表 rd_en/rd_addr/rd_data 均注"来自/送 M3"），§6.3 行 3"返回当前 SRAM 内容"是原课程 spec 的通用性表述，与该结构性端口契约冲突；沿用 BUG-003 裁决确立的"具体结构条文优先于概略描述"原则，应以端口架构为准收窄 §6.3。(2) §11.2 Lab1 验收范围（必做 1/2/3 + 选做 4/5）无任一项要求"APB 读 PKT_MEM 返回真实内容"——必做 2 只测写通路（wr_en/wr_addr/wr_data，§11.2 必2、§12.3 Lab1 细目同），必做 3 测 RES_* 只读通路，选做 4 测 PSLVERR、选做 5 测 IRQ；附录 A/B 全部示例经 RES_* 寄存器读回结果，无一处经 APB 读回 PKT_MEM 原始内容。故该行为对验收无功能价值。(3) 方向 (a) 需给 M1 增 rd_en/rd_addr/rd_data 输出输入端口、并把 M2 单一读端口在 M1(APB) 与 M3(处理) 间做仲裁/复用（"双端口"=1 写+1 读，非 2 读），布线与仲裁复杂度对教学项目为纯负担、零收益，且波及尚未开工的 M3 读端口定义。(4) §0 适配表无相关偏离项，不构成 (a) 的支持。DE 侧临时处理（PSLVERR=0/PRDATA=32'h0，见 apb_slave_if.sv:228/253）恰与本裁决一致，**M1/M2 RTL 无需返工**。建议 spec 修改（待 orch 应用 r7 + pin，应用后本条置 SPEC_CHANGED）：① §6.3 表行 3 改为"APB 读 PKT_MEM｜任意时刻｜PSLVERR=0，PRDATA 返回占位值 32'h0；本仓库 M2 读端口专供 M3，APB 无读回通路，不保证反映 SRAM 真实内容（见 §2.3 M2 表注 r7 / BUG-004）"；② §2.3 M2 表注补一句"读端口仅供 M3，APB 侧无 PKT_MEM 读回通路"；③ §8.3 行 1 保持不变（读 PKT_MEM 仍属合法访问 PSLVERR=0，与本裁决自洽）。受影响清单：testplan 建议新增一行 M1 场景锁定该简化行为（APB 读 0x040~0x05C → PSLVERR=0 且 PRDATA=32'h0），使简化被实测而非默认；design-prompt/apb_slave_if.md 更新为引用 r7（把"BUG-004 OPEN 临时处理"改为 spec 认可行为），apb_slave_if.sv 顶部注释（第 9–13 行 BUG-004 OPEN 说明）建议同步更新为引用 r7 裁决（仅注释、非行为返工，可并入下次 DE 触及本文件时处理）；design-prompt/packet_sram.md 与 packet_sram.sv 无需改动。** **orch 已应用（2026-07-09，spec 修改记录 r7）并重新 pin；testplan 新增 M1-06 锁定该简化行为**。design-prompt/apb_slave_if.md 引用更新与 RTL 注释同步留待下次触及该文件时顺带处理（不阻塞本轮）。 | - | SPEC_CHANGED | - |
| BUG-005 | 2026-07-09 | 0.1.5 | infra（sim 构建，非 spec 歧义） | `sim/Makefile` 的 `FLISTS := -f flist/rtl.f -f flist/tb.f` 顺序把 `rtl.f` 排在 `tb.f` 之前；但 design-prompt 要求 RTL 直接 `import ppa_reg_defs_pkg::*`（CLAUDE.md §1：`tb/uvm/env/ppa_reg_defs.sv` 是地址/常量唯一定义点），该 package 文件本身属于 `tb.f` 清单，晚于 `rtl.f` 编译，导致 `packet_sram.sv` 在 `import` 处报 `Error-[SV-LCM-PND] Package not defined` / `Error-[IND] Identifier not declared`（PKT_MEM_WORDS 未声明） | 取消 `sim/flist/rtl.f` 中 `../rtl/packet_sram.sv` 行注释后执行 `make -C sim compile`，2026-07-09 本地 VM 实测复现（详见 `sim/out/comp.log`） | design-prompt/packet_sram.md「接口/常量定义」一节；CLAUDE.md §1（ppa_reg_defs.sv 唯一定义点） | 待裁决（非 RTL 行为问题，属 sim/Makefile 构建顺序）。DE 未改 `sim/Makefile`/`sim/flist/*`（超出 rtl/ 职责边界，且该文件非本次任务授权修改范围）。候选方案供 orch/rev 参考：①调整 FLISTS 顺序为 `tb.f` 先于 `rtl.f`；②把 `ppa_reg_defs.sv` 抽成独立 `pkg.f`，置于两清单之前统一先编译；③RTL 侧改用 `` `include`` 方式而非 package import（但与设计输入要求的 import 方式冲突，非首选）。本条不影响 RTL 源码正确性，只影响本地自检编译是否能跑通：已用同一份 `packet_sram.sv`（未改动）手动执行 `vcs ... -f flist/tb.f -f flist/rtl.f`（即仅调换两清单顺序，未改 Makefile/flist 文件本身）做诊断性验证，编译 0 error/0 warning（Verdi KDB elaboration："0 error(s), 0 warning(s)"），证实 `packet_sram.sv` 本身语法与 import 无误，问题确系 `sim/Makefile` 里 `FLISTS` 的固定清单顺序所致。**orch 已修复（2026-07-09）**：`sim/Makefile` FLISTS 改为 `-f flist/tb.f -f flist/rtl.f`（方案①），本地 VM 实测 `make -C sim compile` 通过（0 error/0 warning，两模块 apb_slave_if+packet_sram 均入编译），log 见 `sim/out/comp.log`。修复 commit 随下一提交回填（自引用问题：本提交尚不知自身 hash），届时状态置 FIX_READY，待复验关单（关单人≠修复人）。 | 753f676 | CLOSED | doc/evidence/v0.1.6/BUG-005.log |
| BUG-001 | 2026-07-06 | 0.1.0 | spec 歧义 | PKT_LEN_EXP 复位值 0，§9.1 说"若已配置"则不符报 length_error，但"已配置"未定义（0 是否表示未配置？） | 静态歧义，无需仿真 | §5.2 §9.1 | rev 裁决（2026-07-08）：确认 `exp_pkt_len==0` = 未配置、跳过 exp 比对；`!=0` 才做一致性检查。依据：合法 pkt_len∈[4,32]（§3.2），0 恒在合法域外，故 0 作哨兵不与任何合法包冲突；若把 0 当"已配置须匹配"，则 §10.1 N-1/N-2/N-3 未写 PKT_LEN_EXP 的合法包将全部误报 length_error 且 format_ok=0，与其期望结果矛盾；附录 B.1 的一致性检查示例用非零 exp=8 亦印证 exp≠0 才生效。§9.1"若已配置"措辞歧义，需 spec 明文（§9.1 length_error 触发条件、§5.2 PKT_LEN_EXP 说明列同改）。**orch 已应用（2026-07-09，spec 修改记录 r4）并重新 pin；testplan M2-06 已同步**。 | - | SPEC_CHANGED | - |
| BUG-002 | 2026-07-06 | 0.1.0 | spec 歧义 | pkt_len 越界（如 3 或 33）时 res_payload_sum/xor 及读取拍数行为未定义 | 静态歧义，无需仿真 | §3.2 §7.3 §9.2 | rev 裁决（2026-07-08）：非法 pkt_len（<4 或 >32）时 (1) res_payload_sum/res_payload_xor 为 UNSPECIFIED/don't-care，验证侧不比对（依据 §10.2 E-1/E-2 只约束 length_error=1/format_ok=0/done=1/不卡死，未规定 sum/xor，与 §10.1 N-2 明列 sum/xor 形成对照）；(2) 读取拍数必须钳在 8-word 窗口内（§6.1 窗口=0x040~0x05C 共 8 word，rd_addr 3-bit=0..7，§2.3），即 §7.3 的 ceil(pkt_len/4) 读拍公式对 pkt_len>32（如 33 需 9 拍/读 Word8）越界，须钳到 ≤8 拍以免读越 SRAM 窗口/卡死。length_error 于第 0 拍即判定（§7.3）。§7.3 读时序默认 pkt_len∈[4,32]，须补明非法长的 sum/xor 与读拍钳位规则。**orch 已应用（2026-07-09，spec 修改记录 r5）并重新 pin；testplan M2-02 已同步**。 | - | SPEC_CHANGED | - |
| BUG-003 | 2026-07-09 | 0.1.4 | spec 歧义 | M2 packet_sram 读延迟拍数（rd_en→rd_data）未唯一确定：§2.3/§2.2 称"双端口同步 SRAM"通常蕴含寄存读（后 1 拍有效），但 §7.3 "第 0 拍读 Word0 并同拍提取 pkt_len/pkt_type 等头字段"蕴含同拍组合读，二者矛盾 | 静态歧义，无需仿真（arch 撰写 packet_sram design-prompt 时发现，提案 P-M2-READLAT） | §2.2 §2.3 §6 §7.3 | rev 裁决（2026-07-09）：**读端口=同拍组合读**（rd_en=1 当拍 rd_data=mem[rd_addr] 有效、无 1 拍寄存延迟），写端口维持同步时钟写。依据：(1) §7.3 第 0 拍明文"读 Word0 并同拍提取 pkt_len/pkt_type/flags/hdr_chk、执行长度范围检查[4,32]/类型检查/hdr_chk 校验"——读数据与头字段提取及三类检查须在同一拍完成，仅组合读能成立；若寄存读则第 0 拍只能发 rd_addr、数据要到第 1 拍，§7.3 整张读时序表与"length_error 第 0 拍判定(r5)"均破。(2) §7.3 读拍数=ceil(pkt_len/4)（第 0..N-1 拍）且 §7.4 PROCESS 态 mem_rd_en_o 每拍=1、每拍读 1 word 当拍消费——N word 用 N 拍读完当拍消费只与组合读自洽；寄存读需 N+1 拍或流水，与 §7.3 拍数矛盾。(3) §2.2/§2.3"同步 SRAM"的"同步"限定写端口时钟化，未规定读端口寄存；§2.3 M2 端口表无 rd_data_valid/读延迟信号，读口为简单 rd_en/rd_addr→rd_data 映射。适用"具体条文(§7.3 逐拍数据流)优先于概略模块描述(§2.2 一句话职责)"。(4) §0 适配表无相关偏离项。与 BUG-003 登记的 arch 候选方向(1)一致。建议 spec 修改（待 orch 应用 r6+pin）：§2.3 M2 表补读时序契约注、§7.3 第 0 拍标注同拍组合读；同步 packet_sram.md 读时序约束/写后读断言、未来 packet_proc_core.md §7.3 读拍设计、testplan M1-02 采样拍。**orch 已应用（2026-07-09，spec 修改记录 r6）并重新 pin**。 | - | SPEC_CHANGED | - |
